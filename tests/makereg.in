#!/bin/bash

@GEOMOD_BINARY@ $1 finaloutput=/tmp/regtest.g2

echo `cat /tmp/regtest.g2 | awk -F ' ' '{if (sqrt($1*$1) < 1e-6) printf "0\n"; else printf "%5.5g\n", $1;}' | md5sum | awk -F ' '  '{print $1}'
